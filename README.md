## Environment Setup

1. Ensure Python `3.12` is installed (matches `.python-version`).
2. Install `uv` via pip:

```bash
python -m pip install --upgrade pip
python -m pip install uv
```

3. Sync dependencies:

```bash
uv sync
```
